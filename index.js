const { Plugin } = require('powercord/entities');
const { inject, uninject } = require('powercord/injector');


module.exports = class BubbleChannelPlugin extends Plugin {
    startPlugin() {

    }
};
